# macOS
.DS_Store

# Claude Code project settings (machine-specific tool permissions)
.claude/

# Guard against accidentally committing a plain .env file at the root
# (each subdirectory has its own .gitignore covering its own .env files)
.env
